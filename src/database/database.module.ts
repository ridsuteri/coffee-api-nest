import { Module } from '@nestjs/common';

@Module({})
export class DatabaseModule {
    // dummy function to simulate the register functionality for db
    static register(options: string){
        
    }
}
